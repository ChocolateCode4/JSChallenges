window.onload = () => config();

class Canvas {
 static dom = document.querySelector("canvas");
 static ctx = Canvas.dom.getContext("2d");
}

function config() {
  
}

// global
let canvas = Canvas.dom, ctx = Canvas.ctx;
